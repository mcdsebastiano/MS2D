let r;
let angle = 0;
function setup() {
  setColor(BLACK);
}

function draw() {
  // drawBorder();
  r = Rect(100, 100, 60, 60);
  // points.forEach(p => {
    // Rect(p.x, p.y, 10, 10)
  // })
}

// function update() {
  // draw();
  // r.rotateZ(90);
  // r.rotateZ(angle++, true);
// }

// let points = [];

// function mousePressed(event) {
  // points.push({
    // x: event.clientX,
    // y: event.clientY
  // })

// }
